# Plan Mode — pointer (not the source of truth)

The canonical Plan-mode floor is **`.bob/rules-plan/01-spec-quality.md`**
(loaded with this file). Do not duplicate or paraphrase it here.

For project context, read the source docs directly (do not restate):

- `docs/bob-prompts.md` §3 — the phase-plan generator prompt; §4 — the
  phase-spec contract every phase must satisfy.
- `docs/architecture.md` — the source of truth a spec must never contradict.
- `docs/implementation-plan.md` §2 — the scope cutline (must / should / cut).
- `.bob/rules/` — the always-on floor; `.bob/custom_modes.yaml` — the markdown
  fence and the Plan Council gate.

If this file and `01-spec-quality.md` ever disagree, `01-spec-quality.md` wins.
